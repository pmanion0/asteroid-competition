[project]
name = "pricing-sim-player"
version = "0.1.0"
description = "Asteroid Mining Bidding Simulation — Player Kit"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.1",
    "scikit-learn>=1.4",
]
